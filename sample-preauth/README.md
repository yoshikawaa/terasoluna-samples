## Sample of Cookie based Pre-Authentication

## Use-Case

* Secured `/`
* Non Secured `/nonauth`
  * Button `Valid` to add valid Cookie & redirect to `/`
  * Button `Invalid` to add invalid Cookie & redirect to `/`